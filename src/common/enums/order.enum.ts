export enum OrderStatusEnum {
  PENDING,
  PLACED,
  ON_WAY,
  DELIVERED,
  CANCELED,
  REFUNDED,
}
export enum PaymentTypeEnum {
  CASH,
  CARD,
}
export enum CurrencyTypeEnum {
  USD = 'usd',
  EGP = 'egp',
}
